import { observer } from "mobx-react-lite";
import { FC } from "react";
import s from "./style.module.scss";

interface IProps {

}

export const CultureDetailPage: FC<IProps> = observer((props) => {

  return (
    <>
      CultureDetailPage
    </>
  );
});